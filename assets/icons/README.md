# assets/icons

Generated by `tools/icon-scrape/collect.py` (see that README).

## Cache buckets

```
cache/distributable/   # open-license only (ship-ready)
cache/restricted/      # wiki Mojang-derived — local / preview only
cache/unknown/         # unresolved license (stubs)
```

Do not move files from `restricted/` or `unknown/` into `distributable/` without
an explicit license review.
